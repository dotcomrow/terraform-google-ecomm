"""
The `Inventory` type represents a product's inventory information.
"""
type Inventory {
  isInStock: Boolean!
  stockAvailability: Boolean!
  manageStock: Boolean!
}

extend type Product {
  inventory: Inventory!
}
